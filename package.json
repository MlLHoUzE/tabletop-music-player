{
  "name": "obsidian-ttrpg-music",
  "version": "1.0.0",
  "description": "TTRPG background audio controller with front matter filtering.",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild main.ts --bundle --outfile=main.js --watch --external:obsidian --format=cjs --minify=false",
    "build": "esbuild main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --minify"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "ttrpg",
    "audio"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.0.0"
  }
}
